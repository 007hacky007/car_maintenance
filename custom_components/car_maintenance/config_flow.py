"""Placeholder config flow, implemented in a later task."""

from homeassistant.config_entries import ConfigFlow

from .const import DOMAIN


class CarMaintenanceConfigFlow(ConfigFlow, domain=DOMAIN):
    """Placeholder config flow (implemented in a later task)."""

    VERSION = 1
